version: '3'

services:
  app:
    container_name: renchon
    image: ghcr.io/iamtakagi/renchon:latest
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - external_network

networks:
  external_network:
    external: true